import Enum from './enum.js';

export default class Placeholder extends Enum {
  static LOADING = 'Loading...';
  static EMPTY_EVERYTHING = 'Click New Event to create your first point';
  static EMPTY_FUTURE = 'There are no future events now';
}
