import Enum from './enum.js';

export default class DeleteEmpty extends Enum {
  static EVERYTHING = 'Click New Event to create your first point';
  static FUTURE = 'There are no future events now';
}
