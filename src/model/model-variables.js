/** @typedef {import('./app-modal').default} appModel */

/**
 * @template Item
 * @template {Adapter} ItemAdapter
 * @typedef {import('./model-data').default<Item,ItemAdapter>} ModalData
 */

/**
 * @template Item
 * @template {Adapter} ItemAdapter
 * @typedef {import('./model-info').default<Item,ItemAdapter>} ModelInfo
 */

